import React from 'react';
import {StyleSheet} from 'react-native';

import {AssetIssueProps} from './types';

export const AssetIssue: React.FC<AssetIssueProps> = (props) => {
  return <></>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    color: 'white',
    fontSize: 20,
    fontWeight: '600',
  },
  tabBarStyle: {backgroundColor: 'white'},
});
